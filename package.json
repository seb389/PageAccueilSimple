{
  "name": "pageacceuilsimple",
  "type": "module",
  "version": "0.0.1",
  "engines": {
    "node": ">=22.12.0"
  },
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "preview": "astro preview",
    "astro": "astro",
    "deploy": "astro build && wrangler deploy --config ./dist/server/wrangler.json"
  },
  "dependencies": {
    "@astrojs/cloudflare": "^13.3.0",
    "astro": "^6.2.1"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20260423.1"
  }
}
